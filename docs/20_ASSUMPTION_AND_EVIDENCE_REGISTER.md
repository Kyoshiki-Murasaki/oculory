# 20 — Assumption and evidence register

Status values: `validated` (tested here) · `partially-validated` · `open` (needs model traffic or external input). Every open assumption has a linked stop condition — the point at which continuing product work without resolving it becomes dishonest.

| ID | Claim | Category | Evidence | Confidence | If false | Test | Phase | Status | Stop condition |
|---|---|---|---|---|---|---|---|---|---|
| A-01 | Model-driven agents can produce minable, stable behaviour patterns | traffic realism | controlled `gpt-4.1-mini` task/filesystem/issue runs; preserved fs/issue mining + replay artifacts, small catalogues | medium | miner yields mostly unstable assertions on external servers; review burden explodes | repeat unchanged protocol on an external MCP server and later across models | external model traffic | **partially-validated (local targets)** | Do not generalize beyond the tested local targets |
| A-02 | Trace capture is complete enough for mining (tools, args, per-step state, schema hash) | trace completeness | schema + recorder tests; 72/72 traces validate | high | assertions mis-mine | `rawTraceCheck` in every recording; unit tests | done | validated | — |
| A-03 | Deterministic state verification distinguishes intended outcome from tool-level success | outcome verification | silent_write_failure test: all-ok calls → `verified_failure` | high | ground truth is circular | pipeline tests | done | validated | — |
| A-04 | Anti-overfit rules prevent freezing incidental behaviour | assertion stability | dana/priority overfits caught by holdout in this session, fixed, regression-tested; 100% unmutated pass rate | medium-high | false positives on every phrasing change | anti-overfit tests + holdout replay | done | validated (this domain) | Reopen if any new domain shows unmutated pass rate <100% |
| A-05 | Family grouping is a sufficient MVP substitute for intent clustering | clustering quality | works for catalogue traffic where family is known | medium | imported external traces can't be grouped | embedding/heuristic clustering needed before external import | external import | partially-validated | Block external-trace import until clustering exists |
| A-06 | Replay is reproducible (identical inputs → identical suite + results) | reproducibility | two full experiment runs → identical suite hashes (tested) | high | CI is noise | e2e reproducibility test | done | validated (scripted) | Model traffic requires N≥3 trials + instability quarantine instead |
| A-07 | Mutation detection generalises beyond the original demo server | mutation detection | scripted success on task, filesystem, and issue tracker; all three are still in-repo synthetic targets | medium | product only works on authored fixtures | test one maintained external MCP server over its real transport | external validation | **partially-validated (three local domains)** | Do not claim broad MCP generality before external evidence |
| A-08 | The schema-smoke proxy is a fair lower bound for naive baselines | baseline comparison | proxy implements schema-diff + smoke calls honestly; external tools not run | medium | comparison overstated | docs/19 §3 external run | external | open | No public "beats X" claims until run |
| A-09 | False-positive rate stays near zero as suites grow | false positives | FP=0 on the benign probe; only 1 benign probe exists | medium | alert fatigue kills adoption | add more benign mutations (refactors, comment changes) before v0.2 | post-MVP | partially-validated | ≥3 benign probes before any gate-blocking default |
| A-10 | Human review burden is minutes, not hours | review burden | 12 candidates/92 assertions readable in ~10 min (est., untimed) | medium | workflow abandoned | time a real review session (docs/19 §2) | review | open | Measure before recommending team use |
| A-11 | Inference cost per small controlled run is < $5 on the tested model | inference cost | all preserved `gpt-4.1-mini` runs stayed far below their caps; figures are Oculory estimates, not bills | medium | larger/external catalogues are too expensive | measure external-server and cross-model runs against provider billing | model traffic | **partially-validated** | No general CI-cost guidance from these small estimates |
| A-12 | The CLI is usable without reading the implementation | CLI usability | help text, exit codes, next-step hints; only author has used it | medium | adoption friction | one outside developer completes the quick start unaided | packaging | open | Needed before public release |
| A-13 | English-regex entity extraction suffices for the MVP | product complexity | works on catalogue phrasings incl. holdout re-wordings | medium | arg_equals_entity fails on non-English/odd phrasing → advisory-only fallback engages | extraction unit tests; fallback: assertions degrade to unstable, never false-block | done | partially-validated | Extend before importing external traces |
